FROM node:18-alpine

WORKDIR /app

# 安装依赖（包括开发依赖，因为需要构建）
COPY package*.json ./
RUN npm ci --legacy-peer-deps

# 复制源代码
COPY . .

# 构建前端代码
RUN npm run build

# 创建数据目录
RUN mkdir -p ./data

# 暴露端口
EXPOSE 3001

# 启动命令
CMD ["node", "server/server.js"]
